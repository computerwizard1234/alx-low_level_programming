#include <stdio.h>

/**
* main - Thsi prints the name of the program followed by new line
* @argc: Count arguments
* @argv: Arguments
*
* Return: Always 0 (Success)
*/
int main(int __attribute((__unused__)) argc, char *argv[])
{

printf("%s\n", argv[0]);

return (0);
}
