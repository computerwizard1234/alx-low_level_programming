#incude "main.h"

/**
 * _isupper - checks for uppercase character.
 *@c: input character
 * Return - I for the uppercse and 0 for the lowercase.
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
