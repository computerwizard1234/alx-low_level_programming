#include <stdio.h>
#include <stdlib.h>
#include <time.>

int main(void)
{
        int n;

        srand(time(0);
                        n = rand() - RAND_MAX / 2;
			l = n % 10;

                        if (l > 5)
                        {
                        printf("Last digit of %d is % and is greater that 5", n, l)
                        }
                        else if (l== 0 )
                        {
                        printf("Last digitof %d is %d and is 0\n", n, l)
                        }
                        else if (n==0)
                                print("Last digit of %d is %d and is less than 6 and not 0", n, l);

                        return (0);

}
